/**
 * Represents a folder in Google Drive.
 */
export interface DriveFolder {
  /**
   * Unique identifier for the folder.
   */
  id: string;

  /**
   * Name of the folder.
   */
  name: string;
}
